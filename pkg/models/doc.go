// Package models defines the request/response types that cross the hexagonal
// boundary between the MCP protocol layer and the GCP adapter layer.
// All types are plain Go structs with no external dependencies.
package models
